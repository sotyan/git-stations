・gitのステージングの状況などを確認 
    git status
・ステージング
    git add ファイルのパス
・コミット
    git commit -m "コミットメッセージ"

・ブランチの確認 *が現在いるブランチ
    git branch
・ブランチを新しく作る（ブランチを切る）
    git branch {作るブランチ名}
・ブランチを切り替える
    git switch {移動先のブランチ名}
＊ブランチモデルに従ってブランチの運用をする

